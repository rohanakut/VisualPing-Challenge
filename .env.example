# Copy this file to .env and fill in the real values.
# Never commit the real .env file.

AUTH_USERNAME=changeme
AUTH_PASSWORD=changeme

# Optional tuning (defaults shown match the original script's constants)
REQUEST_DELAY=0.15
MAX_PAGES=1000
REQUEST_TIMEOUT=15
FULL_EXPAND_LIMIT=60
SAMPLE_ONLY_LIMIT=200
KNOWN_TRAP_SAMPLE_LIMIT=10
